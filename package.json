{
  "name": "PelePack",
  "version": "1.0.0",
  "description": "PelePack keeps your food looking great.",
  "author": "Eliezer Eliezer",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "@nuxtjs/axios": "^5.3.6"
  },
  "devDependencies": {
    "css-loader": "^3.4.2",
    "node-sass": "^4.13.1",
    "nodemon": "^1.18.9",
    "push-dir": "^0.4.1",
    "sass-loader": "^8.0.2"
  }
}
